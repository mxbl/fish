set -x ANSIBLE_CONFIG              $HOME/dev/ansible.cfg
set -x ANSIBLE_INVENTORY           $HOME/git/ansible-inventory/out/current
set -x ANSIBLE_ROLES_PATH          $HOME/git/ansible-roles
set -x ANSIBLE_VAULT_PASSWORD_FILE $HOME/.ansible/vault-secret
